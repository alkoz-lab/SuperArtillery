1. The most left and most right players still have their "Direction:" control enabled and visible,
 even though it has no effect for them.
2. "Direction:" values "Left"/"Right" are not clear enought for the players. 
Change them to "Shoot↖️" and "Shoot↗️"? Display direction in Fire button?
3. Rematch bug - the one choosing [I had enough] remains frozen (needs some UI update). 
Another one which chosen [Play again] remains, doesn't take turn. Suggesting that server sends turn message to wrong player.
4. Rematch players list isn't formatted properly. Player number is on the left side, then very large gap before the player 
tatus (emoji) then player name.
5. Feature: count frags for each player and display it in the UI. E.g. "Alex +1". Frags kept on client side. 
Keep accumulated with each rematch.
6. Does game expired if players are connected? Maybe there should be hearbeat message from each player to keep it alive?
7. Skip waiting player button - only playerId 0 should ever see it.
8. When player drop, playeId may change. That breaks game for pre-existing players whose Id changed. Drop playerId?