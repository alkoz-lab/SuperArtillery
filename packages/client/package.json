{
  "name": "superartillery-client",
  "private": true,
  "version": "1.5.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "generate:contracts": "npm --prefix ../.. run contracts:generate",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@superartillery/core": "*",
    "@types/node": "^25.0.3",
    "jsdom": "^26.1.0",
    "typescript": "^7.0.2",
    "vite": "^7.2.4",
    "vitest": "^4.1.11"
  }
}
