{
  "name": "@superartillery/core",
  "version": "0.1.0",
  "private": true,
  "description": "Transport-independent SuperArtillery game core, runnable in Node and the browser",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "type-check": "tsc --build --force",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  },
  "license": "ISC"
}
